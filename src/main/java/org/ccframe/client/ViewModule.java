package org.ccframe.client;

import com.google.gwt.inject.client.AbstractGinModule;

public class ViewModule extends AbstractGinModule {

	@Override
	protected void configure() { //无需配置
//		bind(ArticleInfMgrView.class).in(Singleton.class);
//		bind(ArticleInfWindowView.class).in(Singleton.class);
//		bind(AdminUserWindowView.class).in(Singleton.class);
	}

}
